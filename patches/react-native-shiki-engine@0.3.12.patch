diff --git a/android/CMakeLists.txt b/android/CMakeLists.txt
--- a/android/CMakeLists.txt
+++ b/android/CMakeLists.txt
@@ -3,12 +3,12 @@
 
 set(CMAKE_VERBOSE_MAKEFILE ON)
 
-# Find prebuilt oniguruma library
+# Link the ABI-specific Oniguruma binary shipped with the package. Using an
+# imported target avoids CMake caching or discovering a host libonig.
 find_library(LOG_LIB log)
-find_library(ONIG_LIB onig
-    PATHS ${CMAKE_CURRENT_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}
-    NO_CMAKE_FIND_ROOT_PATH
-    REQUIRED
+add_library(shiki_engine_oniguruma SHARED IMPORTED)
+set_target_properties(shiki_engine_oniguruma PROPERTIES
+    IMPORTED_LOCATION "${CMAKE_CURRENT_SOURCE_DIR}/src/main/jniLibs/${ANDROID_ABI}/libonig.so"
 )
 
 # Main library
@@ -65,7 +65,7 @@
 
 # Link libraries
 target_link_libraries(react-native-shiki-engine
-    ${ONIG_LIB}
+    shiki_engine_oniguruma
     ${LOG_LIB}
     android
     fbjni::fbjni
